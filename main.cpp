/*


              _,-'/-'/
  .      __,-; ,'( '/
   \.    `-.__`-._`:_,-._       _ , . ``
    `:-._,------' ` _,`--` -: `_ , ` ,' :
       `---..__,,--'            ` -'. -'

Mikaela Diaz
*/

#include <iostream>
#include "DigitalRain.h"

int main() {
    int width = 80; // Increase the width
    int height = 20;
    int numRaindrops = 5;

    simulateRainfall(width, height, numRaindrops);

    return 0;
}
